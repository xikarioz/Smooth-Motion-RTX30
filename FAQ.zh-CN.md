[English](FAQ.md) | 简体中文 | [Español](FAQ.es-419.md)

# 常见问题

**我需要 Python 吗？**
不需要。发行包是自包含的。

**我需要哪种 GPU/驱动程序？**
目标架构：RTX 30 系列 / Ampere **SM86**。**RTX 3090 + 驱动程序 616.64** 已通过
实机验证；在精确的驱动程序/配置文件被接受的情况下，其他单 GPU 的 SM86 RTX 30
显卡为**实验性**。其他驱动程序版本和非 SM86 GPU 会被拒绝。

**我有两块 GPU —— 我的 RTX 30 就不受支持吗？**
不是。多 GPU 系统：当前消费者预览版期望单一的 CUDA/NVIDIA 目标设备；显式的
渲染 GPU 选择计划在后续版本中提供。这是管理器设备选择方面的限制，而非硬件
不兼容。

**它会更改我的驱动程序吗？**
不会。它读取你已安装的驱动程序，并在 `%LOCALAPPDATA%` 下准备经适配的运行时
状态。DriverStore 永不被修改。

**它会下载任何内容吗？**
不会。它完全离线工作。没有任何遥测。

**它是 DLSS 帧生成模组吗？**
不是。它针对的是 NVIDIA 的驱动级 NvPresent / Smooth Motion 路径，而不是
Streamline/NGX DLSS-G。

**D3D11 与 D3D12 的验证方式相同吗？**
不相同，本项目将二者明确区分。在黄金配置上，**D3D11 实时 开/关/开 路径已通过
仪器验证**。**D3D12** 标题被**观察到在真实游戏中可用**，但当前活动的动态引擎
运行时仪器化是**尚无定论**（在最近的验证窗口内未观察到目标模块加载流量；未归类
为回归）。见 `VALIDATION.md`。

**我可以在在线/竞技游戏中使用它吗？**
不可以。仅限单人/离线。受反作弊保护的游戏不在范围内。

**为什么我会看到伪影或额外延迟？**
这是时间插值；质量取决于基础帧率。在极低的基础帧率下（在我们的测试中约为
20 FPS），伪影与可感知延迟会显著增加。强烈建议使用更高的基础 FPS。

**游戏启动了，但没有任何变化。**
请确认你是通过应用启动的、原生帧生成已关闭，并检查 **状态**。会重启为新进程的
游戏会丢失按进程激活。

**如何移除它？**
已安装版本：设置 → 应用 → *Smooth Motion SM86*。便携版本：`Uninstall.cmd`
（可选 `Uninstall.cmd -IncludeGenerated` 以同时移除生成的运行时副本）。

**我可以再分发它吗？**
不可以。仅限个人使用；不得再分发或重新打包。见 `EULA.txt`。

**如何确认下载是正版？**
将 `SHA256SUMS.txt` 中的 SHA-256 与发行页面进行核对。只从官方仓库的 Releases
下载。
