[English](SUPPORT.md) | 简体中文 | [Español](SUPPORT.es-419.md)

# 支持状态

Smooth Motion SM86 面向一个有意保持窄范围、经过验证的架构。安装程序和管理器在
未知驱动程序上同样可以运行；Smooth Motion 激活会被拒绝（故障关闭），直到你安装的
NvPresent 二进制文件被识别并通过验证。

跟踪三个彼此独立的维度（不要将它们合并）：

- **硬件兼容性** — 引擎可以针对的 GPU 架构（Ampere SM86）。
- **实机验证** — 实际测试过的确切显卡/驱动程序组合。
- **多适配器产品支持** — 当存在多个 GPU 时，当前管理器能否*选择*正确的
  目标设备。

## 黄金配置（已实机测试）

| 项目 | 状态 |
|---|---|
| GPU | NVIDIA RTX 3090（GA102，Ampere SM86），单一活动适配器 |
| 驱动程序 | 616.64（`32.0.16.1664`） |
| 操作系统 | Windows 10/11 x64 |
| D3D11 实时路径 | **仪器验证** — 在黄金配置上实时 ON/OFF/ON |
| D3D12 真实游戏路径 | **观察到可用** — 动态引擎运行时的活动插桩目前**尚无定论** |
| 安装 / 准备 / 回滚 / 卸载 | 支持 |

## 实验性

| 项目 | 说明 |
|---|---|
| 其他单 GPU 的 Ampere SM86 RTX 30 显卡（RTX 3080 / 3080 Ti / 3070 / 3060 / 3050，含笔记本）搭配驱动程序 616.64 | 相同架构；在确切的驱动程序/配置文件被接受时**技术上予以接纳**，未经实机验证。可能可用；不作承诺。 |
| Windows Vulkan | 研究路径已演示；打包的激活为实验性，可能无法在商店前端进程重启后继续生效。 |
| Vulkan 路径上的游戏内切换 | 未实现。 |

## 不支持

- 在未知/未验证的 NvPresent 版本上激活 Smooth Motion（安装程序和管理器仍可用；激活为故障关闭）。
- 非 SM86 GPU（RTX 40/50、RTX 20、GTX 10、AMD/Intel）。
- 32 位游戏。
- 受反作弊保护或竞技性多人游戏。
- 自动更新（无；请手动下载新版本）。

## 设备选择（并非硬件限制）

多 GPU 系统：当前消费者预览版期望单一的 CUDA/NVIDIA 目标
设备。显式的渲染 GPU 选择计划在后续版本中提供。

这是**管理器设备选择方面的限制**，并不表示那些 GPU
不兼容。引擎的架构接纳与已安装的适配器数量
无关。

## 兼容性如何判定

本工具按顺序验证：针对你的驱动程序 NvPresent 二进制文件的已审核配置文件、
确切的二进制哈希、确切的 Windows 驱动程序版本、恰好一个 CUDA 设备，
以及确切的 SM86 计算能力。任何不匹配都会以明确的原因
终止流程。

## 社区层级

| 层级 | 含义 |
|---|---|
| `PROJECT_VALIDATED` | 由项目在参考显卡上验证 |
| `COMMUNITY_CONFIRMED` | 多份带可信证据的独立用户报告 |
| `COMMUNITY_REPORTED` | 可信的用户报告，插桩不完整 |
| `EXPERIMENTAL` | 架构兼容，尚未验证 |
| `UNTESTED` | 暂无数据 |

单份用户报告绝不会将某个配置提升为 `PROJECT_VALIDATED`。有关报告架构以及
商店前端 / 启动方式 / 基础 FPS 这些维度，请参阅
[COMMUNITY_VALIDATION.md](docs/COMMUNITY_VALIDATION.md)。

## 报告结果

提交一份
**[硬件验证报告](https://github.com/xikarioz/Smooth-Motion-RTX30/issues/new?template=hardware_validation.yml)**，
并附上 GPU、驱动程序、Windows 版本、游戏、商店前端、图形 API 以及导出的
诊断信息（管理器中的**导出诊断信息**，或 `sm86.exe report`）。报告
本身绝不会将某个配置提升为「已验证」。
