[English](TROUBLESHOOTING.md) | 简体中文 | [Español](TROUBLESHOOTING.es-419.md)

# 疑难解答

## 我关闭了窗口，但程序仍在运行

这是有意设计：标题栏的 **X** 会将 Smooth Motion SM86 最小化到**系统托盘**并保持运行，
以便继续进行后台监控和实时控制。窗口只是隐藏了，并未退出。

若要完全关闭：右键单击**托盘图标**并选择**退出**。只有这样，管理器进程、托盘图标和
全局快捷键才会停止。（如果托盘图标被 Windows 隐藏，请展开任务栏中的隐藏图标。）

## 兼容性检查显示我的 GPU/驱动程序不受支持

安装不会因驱动程序版本而被阻止：安装程序和管理器仍可运行，Smooth Motion 激活会被拒绝
（故障关闭），直到你安装的 NvPresent 二进制文件被识别并通过验证。项目只对一块显卡
（RTX 3090）搭配驱动程序 616.64 进行实机验证；特定已识别的 **591.86** NvPresent 版本为
**静态验证 / 实验性**。如果你拥有一块 RTX 30 / SM86 GPU，在精确的驱动程序/配置文件被识别时，
它会被作为**实验性**予以接纳（而非「不支持」）。

## 我有两块 GPU（多 GPU）

当前消费者预览版期望单一的 CUDA/NVIDIA 目标设备。显式的渲染 GPU 选择计划在后续版本中提供。
这是**管理器设备选择方面的限制，而不是硬件不兼容** — 目标架构（Ampere SM86 / RTX 30 系列）
不受已安装适配器数量的影响。

## 游戏启动了，但我看不出差别

1. 检查 **状态** — `null` 表示未知，绝不是 False。
2. 在游戏菜单中关闭游戏**原生**的帧生成；不要叠加其他帧生成模组。
3. 确保游戏是通过应用的 **启动游戏**（或 `sm86.exe launch`）启动的。注入是按进程进行的；
   自行重启为新进程的游戏会失去注入 — 自动跟随尚未启用。
4. 测量时使用无边框/窗口模式；推荐使用游戏内 FPS 计数器加 NVIDIA 叠加层作为读数。

## 伪影很多或延迟很高

在我们的测试中，基础帧率极低时（约 20 FPS），插值伪影与可感知延迟会显著增加。
在开启 Smooth 之前先提高基础帧率。不要指望 Smooth 能让 20 FPS 以完美画质变得像 40 FPS。

## Windows Vulkan 游戏

Vulkan 路径为实验性。部分商店游戏（例如 Game Pass）会重启为全新的游戏进程，从而使按进程激活
失效；这些目前尚不支持。见 [SUPPORT.md](SUPPORT.zh-CN.md)。

## 「未知驱动程序」/ 没有任何修改

正确——这也是预期行为。未知驱动程序不会被修改；本工具从不猜测。安装程序和管理器仍可运行；只有 Smooth Motion 激活会被拒绝（故障关闭），直到你的 NvPresent 二进制文件被识别并通过验证。

## 彻底移除

- `Uninstall.cmd` — 移除应用文件 + 快捷方式（保留生成的运行时副本）。
- `Uninstall.cmd -IncludeGenerated` — 同时移除生成的副本。
- `sm86.exe rollback` — 仅移除生成的副本。

卸载程序会拒绝 `%LOCALAPPDATA%\SmoothMotionSM86` 之外的任何路径。

## 报告问题

从应用中导出诊断信息（或运行 `sm86.exe report`）。它会创建一个仅包含安全元数据的 ZIP —
诊断、哈希、配置文件、高层状态 — 并将个人路径脱敏。其中不包含任何 NVIDIA 二进制文件。
附加前请先检查。请附上 GPU、驱动程序、Windows 版本、游戏、API，以及是否关闭了原生帧生成。
