[English](VALIDATION.md) | 简体中文 | [Español](VALIDATION.es-419.md)

# 验证

本项目的兼容性引擎为专有实现，因此本文档发布的是**证据**，而非方法。它不包含任何偏移量、地址、签名、钩子内部实现、分类器内部实现或 NVIDIA 二进制文件——只有高层级、可公开安全的结果。

本文档是项目证据分级措辞的**规范来源**。其他文档链接到此处，而不重复那些有细微差别的声明。

## 黄金系统

| 项目 | 值 |
|---|---|
| GPU | NVIDIA GeForce RTX 3090（GA102，Ampere SM86），单一活动适配器 |
| 驱动程序 | 616.64（`32.0.16.1664`） |
| 操作系统 | Windows 10/11 x64 |
| 图形 API | 原生 D3D11 与 D3D12（打包产品）；实验性 Vulkan |
| 安装 / 准备 / 回滚 / 卸载 | 支持 |

其他单 GPU 的 SM86 RTX 30 显卡架构兼容，在实机验证前为**实验性**。特定已识别的 **591.86** NvPresent 版本为**静态验证 / 实验性**（尚未实机验证）。在未知或未验证的 NvPresent 版本上，安装程序和管理器仍可运行，Smooth Motion 激活会被拒绝（故障关闭，不会修改任何内容）。

## 兼容性适配

在已验证的黄金构建上，所需适配紧凑且以元数据为范围：

- **41 处修改位点 / 共 61 字节**
- 1 个主机架构资格字节
- 60 字节 GPU 模块元数据
- **20 个兼容的非 FP8 模块目标**已适配
- **17 个面向 FP8 的模块被有意排除**——Ampere SM86 缺少这些模块所针对的原生 FP8 Tensor Core 执行路径
- 在已验证的黄金配置上，**无需测试任何非 FP8 SASS 指令重写**

「紧凑」描述的是已验证结果；它并非数学意义上的最小性声明。此处不发布任何补丁偏移量、签名或地址表。

## 运行时验证

证据在运行时层面收集，仅作高层级描述：

- **真实运行时 CUDA 模块观测**——真实会话实际加载的 CUDA 模块会被观测，并与静态总体进行核对。
- **影子变换一致性**——在授予任何运行时权限之前，将独立推导与已验证构建进行逐字节比较。
- **活动动态引擎验证**——引擎驱动真实会话并产生预期变换，且没有任何非零 CUDA 结果。
- **精确回滚检查**——每次激活都可逆，回读经验证可返回原始状态。
- **ON/OFF/ON 对照**——在 D3D11 路径上跨保持阶段跟踪实时已请求/已应用转换。
- **生命周期测试**——重复的附加/初始化循环，无权限泄漏，无双重挂钩。

当前研究引擎也已在运行时针对已知良好的 RTX 3090 / 616.64 配置完成验证，包括变换一致性、事务性回滚和重复的实时状态转换。

## 黄金引擎结果

| 结果 | 值 |
|---|---|
| 实时权限转换 | **300** |
| 状态不匹配 | **0** |
| 回滚 | 精确 |
| 生命周期 | 通过 |
| D3D11 A/B/A | 通过 |
| D3D12 | 无结论——在测试窗口内未观测到运行时模块加载流量；不声称已通过运行时验证 |

## 证据分级

项目绝不混淆这些状态：

- *已请求* ≠ *已应用 / 已激活* ≠ *后端执行中* ≠ *输出已验证*
- 「观察到可用」（在真实游戏中的直接用户观察）不同于「仪器验证」（自动化帧内容证据）。
- 一个已验证的（GPU、驱动程序、API、启动方式、游戏）组合不会被推广到未测试的组合。
- 社区分级：`PROJECT_VALIDATED` · `COMMUNITY_CONFIRMED` · `EXPERIMENTAL` · `UNTESTED`。单份用户报告绝不会将某个配置提升为 `PROJECT_VALIDATED`。

有关产品分级，请参阅 [SUPPORT.zh-CN.md](SUPPORT.zh-CN.md)、[COMPATIBILITY.zh-CN.md](COMPATIBILITY.zh-CN.md) 和 [RELEASE_NOTES.zh-CN.md](RELEASE_NOTES.zh-CN.md)。

## 有意不发布的内容

兼容性引擎的实现是私有的。本文档有意省略钩子实现、地址、偏移量、签名、分类器内部实现以及任何原始 NVIDIA 二进制内容。不重新分发任何 NVIDIA 二进制文件。

> 本译文仅为方便而提供。如措辞存在差异，以英文版本为准（英文版本是本项目的规范文档）。
