[English](RELEASE_NOTES.md) | 简体中文 | [Español](RELEASE_NOTES.es-419.md)

# 发行说明 — Smooth Motion SM86 0.4.4（本地化）

> **打包说明：** 在 v0.4.3 发行版中，便携式运行时资源仍为
> `SmoothMotionSM86-0.4.2-win64.zip`；v0.4.3 是在未更改的 0.4.2 引擎之上的
> 安装程序/呈现更新。该资源名称是有意为之，并非过期。

## 证据等级（请先阅读）

本项目始终对这些等级加以区分，绝不合并：

- **D3D11 — 仪器验证的实时路径。** 在黄金配置上，通过运行时仪器对实时 ON/OFF/ON
  进行了验证。
- **D3D12 — 在实际游戏中观察到可用，活动引擎仪器验证尚无定论。** Smooth Motion
  在实际 D3D12 游戏中观察到可用，但当前动态引擎运行时仪器验证**尚无定论**，因为在
  最近的活动引擎验证窗口内未观察到目标模块加载流量。这**不**归类为回归。
- **Vulkan — 实验性。** 无游戏内开关。

规范来源：[VALIDATION.md](VALIDATION.md)。

## 0.4.4 的新功能

- **全面本地化：English、简体中文和 Español。**
- **管理器 / 启动器** — 标头中的语言选择器；切换立即生效，并会保存所选语言。
  首次运行遵循 Windows UI 语言。
- **安装程序** — English / 简体中文 / Español，从 Windows UI 语言或语言对话框中选择；
  所选语言会在首次运行时带入管理器。
- **系统托盘、Windows 通知和 CLI** 使用所选语言（机器可读的 JSON 保持语言中立）。
- **公开文档** — README、安装指南、故障排除、支持、兼容性、FAQ、路线图、验证、
  隐私、安全和 EULA 均提供全部三种语言版本。

## 0.4.3 的新功能

- **安装程序中的全新系统兼容性页面。** 在安装任何内容之前，安装程序现在会显示检测到的
  **GPU**、**NVIDIA 驱动程序**、**体系结构**以及通俗易懂的兼容性状态：
  - `Compatible`（已验证配置），
  - `Experimental`（体系结构兼容但未经物理验证），
  - `Driver not yet supported`（你的驱动程序版本尚未验证 — 不会修改任何内容），
  - `Multiple GPUs`（此预览版不支持自动目标选择），
  - `Not supported`（超出 RTX 30 / Ampere SM86 范围）。
- 一个小型 **Technical details** 按钮显示 GPU 型号、体系结构、计算能力和驱动程序版本
  （无内部数据）。
- 静默安装（`/VERYSILENT`）不受影响，绝不会在该页面等待。

产品引擎未更改：随附的应用程序仍是同一个已验证的 0.4.2 版本。
本次发行是安装程序/UX 改进。

## 0.4.2 的新功能

- **系统托盘控件**：状态、当前游戏、一键 **Toggle Smooth Motion**、打开管理器、
  诊断信息、退出。将管理器关闭到托盘，继续游戏。
- **原生 Windows 通知**：当你实时切换（托盘、快捷键或管理器）时，会显示一条常规
  Windows 通知，内容为 `Smooth Motion ON` / `Smooth Motion OFF` 以及游戏名称。
  失败情况也会通知（无活动游戏、实时不可用、状态未验证）。
- 托盘、管理器、快捷键和 CLI 保持为**同一个后端**（`runtime.toggle`）；每个界面都读取
  相同的已验证状态。
- 实时开关：**D3D11 仪器验证**；**D3D12 观察到可用，但活动引擎仪器验证目前尚无定论**
  （见 *证据等级*）。Vulkan 会话显示“实时开关不可用”（绝不伪造）。

## 0.4.1 的新功能

- **对运行中游戏的实时 ON/OFF 控制。** 当受支持的游戏被注入并运行时，管理器会显示一张
  **NOW PLAYING** 卡片，带有一键 ON/OFF 按钮，可立即更改正在运行的游戏 — 无需终端，
  无需重启。非常适合同场景 A/B/A 对比。
- **可选全局快捷键**（默认为 **Ctrl + Alt + S**，可在设置中配置），无需离开游戏即可
  实时切换。
- 实时控制使用与 CLI（`on`/`off`）相同的已验证运行时引擎，并在报告成功前通过回读
  验证更改。
- 实时开关：**D3D11 仪器验证**；**D3D12 观察到可用**（活动引擎仪器验证目前尚无定论）。
  Vulkan 会话**不**启用它（显示为“此会话不可用”）。
- 仅分发式打包、管理器、游戏库和驱动程序变更检测，与 0.4.0 相同。

## 本工具是什么

一款未签名的研究预览工具，可在 Ampere SM86 GPU（RTX 30 系列）上启用 NVIDIA Smooth
Motion，主要在配备驱动程序 616.64 的 RTX 3090 上验证。它从你自行安装的 NVIDIA
驱动程序中获取所需内容；不重新分发任何 NVIDIA 二进制文件，且绝不修改 DriverStore。

## 0.4.0 的新功能

- **Smooth Motion 管理器**，带有总开关和按游戏开关，均可一键操作。
- **游戏库**扫描（Steam、Epic、Game Pass）— 选择游戏并按下 Play。
- **自动系统检测**，提供通俗易懂的兼容性状态。
- **驱动程序变更检测**：旧配置文件绝不会应用于已变更的驱动程序。
- **仅分发式打包**：发行版配置文件数据嵌入在编译后的产品中；不附带可读的配方文件。
- 专有最终用户许可（源代码不发布）。

## 已验证环境

- NVIDIA RTX 3090（SM86），单个活动适配器
- 驱动程序 616.64（`32.0.16.1664`），Windows 10/11 x64

## 状态

- D3D11：选定游戏，直接 EXE 启动 — **仪器验证**（在黄金配置上的实时 ON/OFF/ON）。
- D3D12：选定游戏 — **观察到可用**；动态引擎运行时仪器验证目前**尚无定论**
  （并非回归）。
- 体系结构兼容（实验性）：使用相同驱动程序的其他单 GPU RTX 30 / SM86 显卡 — 相同
  体系结构，未经物理验证。
- 实验性：Windows Vulkan。
- 不支持：其他驱动程序版本、32 位游戏、反作弊游戏。
- 多 GPU：当前消费者预览版需要单个 CUDA/NVIDIA 目标设备。明确的渲染 GPU 选择计划在
  后续发行中提供（这是管理器设备选择的限制，并非硬件不兼容）。

## 已知限制

- 未签名构建（`UNSIGNED_RESEARCH_PREVIEW`）。
- 会重新启动到新进程的商店平台游戏（Game Pass / 部分 Epic）目前尚无法可靠跟踪。
- Vulkan 路径上没有游戏内开关。
- 强烈建议较高的基础 FPS；极低的基础 FPS 会增加伪影和可感知延迟。
