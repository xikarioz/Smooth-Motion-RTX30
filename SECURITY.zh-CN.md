[English](SECURITY.md) | 简体中文 | [Español](SECURITY.es-419.md)

# 安全

## 预期用途

仅限单人 / 离线使用。请**勿**用于受反作弊保护或竞技性多人游戏。本工具会进行文件名启发式
检查并拒绝明显的反作弊组件，但任何启发式检查都不是保证。

## 本工具会做什么

- 读取你已安装的 NVIDIA 驱动程序，以验证 GPU、驱动程序版本与二进制文件哈希。
- 根据你自己的驱动程序，在 `%LOCALAPPDATA%\SmoothMotionSM86` 下准备已适配的运行时状态。
  原始驱动程序文件不会被改动。
- 在启动时将准备好的状态加载到游戏进程中，或（实验性 Vulkan）仅在内存中适配已加载的
  已签名后端。

## 本工具不会做什么

- 不重新分发 NVIDIA 二进制文件。
- 不修改 DriverStore。
- 不需要管理员权限。
- 无遥测、无网络访问、无账户。
- 不修改 NVIDIA App 或 `nvdrsdb`。

## 故障关闭式行为

未知驱动程序、未知哈希、意外布局、不受支持的 GPU 或含糊的配置文件数据：操作会以明确的
原因停止，且不留下部分状态。不存在「尽力而为」模式。

## 备份与回滚

- 按版本保留应用副本；不覆盖正在使用的安装。
- `Uninstall.cmd` 仅移除项目自有的文件（带路径防护）。
- 回滚不会触碰你的 NVIDIA 驱动程序 — 它从未被修改。

## 诊断信息

`sm86.exe report` 会生成一个仅含安全元数据的本地 ZIP。它绝不包含 NVIDIA 二进制文件、
内存转储或凭据，并会对个人路径脱敏。不会自动上传任何内容。

## 报告漏洞

请通过仓库的 GitHub Security Advisories 私下报告安全问题，而不要使用公开问题。
请附上版本、复现步骤，以及是否有任何 DriverStore 或厂商文件被修改。

## 已知边界

- 未签名构建；请验证发行版的 SHA-256。SmartScreen 可能会提示。
- 反作弊扫描是启发式的，并非保证。
- 不信任第三方重新打包；只从官方 Releases 下载。

> 本译文仅为方便而提供。如措辞存在差异，以英文版本为准（英文版本是本项目的规范文档）。
